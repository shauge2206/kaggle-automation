

service: kaggle-automation

frameworkVersion: '3'

provider:
  name: aws
  runtime: dotnet6

package:
  individually: true

functions:
  kaggleIntegration:
    handler: CsharpHandlers::AwsDotnetCsharp.Handler::Hello
